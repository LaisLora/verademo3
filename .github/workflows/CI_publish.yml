on:
  push:
    branches:
      - main
jobs:
  call:
    uses: LaisLora/github-actions-templates/.github/workflows/template_veracode_fluxo.yml@main
    with:
      build-id: ${{ github.run_id }}.${{ github.run_number }}
      environment: dev
      project_veracode: verademo3
      veracode_policy_name: AFRIKA_AST
    secrets: inherit  
 
